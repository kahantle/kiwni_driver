import 'package:flutter/cupertino.dart';

class IncentiveFragment extends StatefulWidget {
  const IncentiveFragment({Key? key}) : super(key: key);

  @override
  State<IncentiveFragment> createState() => _IncentiveFragmentState();
}

class _IncentiveFragmentState extends State<IncentiveFragment> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("incentive fragment"),
    );
  }
}
